<h1>新規投稿</h1>

<!-- form_for部分 endまで有効 -->
<!-- form_fotは標準で、/posts のroutesに値を渡す。 -->
<%= form_for(@post) do |f| %>

	<h4>タイトル</h4>
	<%= f.text_field :title %>

	<h4>本文</h4>
	<%= f.text_area :body %>

	<%= f.submit '投稿' %>

<% end %>
